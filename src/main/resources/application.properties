spring.application.name=cineplus

# Datasource (update these values for your local DB)
spring.datasource.url=jdbc:mysql://localhost:3306/cineplus_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Diego2004.

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# JWT settings
app.jwt.secret=replace-with-a-secure-random-secret
app.jwt.expiration-ms=3600000

# Server
server.port=8080
